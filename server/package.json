{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --project server.tsconfig.json --watch '**/*.ts' index.ts | ./node_modules/.bin/pino-pretty",
    "build": "tsc -p server.tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "deploy": "echo 'not implemented'"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/pino": "^6.3.6",
    "pino-pretty": "^4.7.0",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.6"
  },
  "dependencies": {
    "@prisma/client": "^2.18.0",
    "apollo-server": "^2.21.0",
    "argon2": "^0.27.1",
    "class-validator": "^0.13.1",
    "graphql": "^15.5.0",
    "pino": "^6.11.1",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "^1.1.1"
  }
}
